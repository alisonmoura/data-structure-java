package Exercises.SumSinglyLinkedList;

import LinkedList.SinglyLinkedList.Node;
import LinkedList.SinglyLinkedList.SinglyLinkedList;

public class SumSinglyLinkedListImpl<T extends Number> implements SumSinglyLinkedList<T>{

    @Override
    public int sumSeq(SinglyLinkedList<T> list) {

        int sum = 0;
        T first = list.first();


        return 0;
    }

    @Override
    public int sumRec(SinglyLinkedList<T> list) {

            return 0;
    }

}
